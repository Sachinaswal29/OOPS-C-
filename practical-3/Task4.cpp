#include<iostream>
#include<string.h>
using namespace std;
int main(){
    string a;
    cout<<"Enter\n";
    getline(cin,a);
    cout<<a;
    return 0;
    }
    
//cin
//It is used to read a word or character.
//It terminates on encountering whitespace or endline character('\n')

//The C++ getline() is a standard library function that is used to read a string or a line from an input stream.
//It is a part of the <string> header.The getline()function extracts characters from the input stream and appends it to the string object until the delimiting character is encounterd
